name: "Immich - BoringCache"

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/immich-boringcache.yml"
      - ".github/workflows/immich-actions-cache.yml"
      - "dockerfiles/immich/**"
      - "scripts/**"
  schedule:
    - cron: "0 6 * * 0"

permissions:
  contents: read
  actions: read

env:
  PROJECT_REPO: immich-app/immich
  PROJECT_REF: "dd97395f3ac72975af2daec584ebd29dac1f7f66"
  BENCHMARK_ID: immich
  BENCHMARK_WORKSPACE: boringcache/benchmarks

jobs:
  seed-cache:
    name: Docker Seed (BoringCache, cold)
    runs-on: ubuntu-latest
    timeout-minutes: 180
    outputs:
      cache_scope: ${{ steps.scope.outputs.cache_scope }}
      cold_seconds: ${{ steps.cold_baseline_build.outputs.seconds }}
      tags_csv: ${{ steps.scope.outputs.tags_csv }}
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          repository: ${{ env.PROJECT_REPO }}
          ref: ${{ env.PROJECT_REF }}

      - name: Checkout benchmark config
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.sha }}
          path: .benchmark-config

      - name: Setup BoringCache CLI
        uses: boringcache/setup-boringcache@v1
        with:
          token: ${{ secrets.BORINGCACHE_API_TOKEN }}
          platform: debian-bookworm-amd64
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}

      - name: Prepare benchmark build inputs
        id: scope
        run: |
          cp "$(which boringcache)" ./boringcache-bin
          echo '!boringcache-bin' >> .dockerignore
          echo '!libjxl-patches' >> .dockerignore
          cp .benchmark-config/dockerfiles/immich/Dockerfile.boringcache Dockerfile.boringcache
          cp -r .benchmark-config/dockerfiles/immich/libjxl-patches ./libjxl-patches

          cache_scope="${BENCHMARK_ID}-run-r${GITHUB_RUN_ID}-a${GITHUB_RUN_ATTEMPT}"
          docker_registry_tag="${BENCHMARK_ID}-docker-layers"
          pnpm_tag="immich-pnpm"
          mise_tag="immich-mise"
          tag_libheif="immich-libheif-1.20.2"
          tag_libjxl="immich-libjxl-0.11.1"
          tag_libraw="immich-libraw-0.22.0"
          tag_imagemagick="immich-imagemagick-7.1.2-2"
          tag_libvips="immich-libvips-8.17.3"
          tag_ffmpeg="immich-ffmpeg-7.1.3-1"
          internal_platform_suffix="debian-13-x86_64"
          purge_internal_tags_csv="${pnpm_tag}-${internal_platform_suffix},${mise_tag}-${internal_platform_suffix},${tag_libheif}-${internal_platform_suffix},${tag_libjxl}-${internal_platform_suffix},${tag_libraw}-${internal_platform_suffix},${tag_imagemagick}-${internal_platform_suffix},${tag_libvips}-${internal_platform_suffix},${tag_ffmpeg}-${internal_platform_suffix}"
          purge_registry_tags_csv="${docker_registry_tag}"
          tags_csv="${pnpm_tag},${mise_tag},${tag_libheif},${tag_libjxl},${tag_libraw},${tag_imagemagick},${tag_libvips},${tag_ffmpeg},${cache_scope},${docker_registry_tag}"
          echo "cache_scope=${cache_scope}" >> "$GITHUB_OUTPUT"
          echo "docker_registry_tag=${docker_registry_tag}" >> "$GITHUB_OUTPUT"
          echo "pnpm_tag=${pnpm_tag}" >> "$GITHUB_OUTPUT"
          echo "mise_tag=${mise_tag}" >> "$GITHUB_OUTPUT"
          echo "tag_libheif=${tag_libheif}" >> "$GITHUB_OUTPUT"
          echo "tag_libjxl=${tag_libjxl}" >> "$GITHUB_OUTPUT"
          echo "tag_libraw=${tag_libraw}" >> "$GITHUB_OUTPUT"
          echo "tag_imagemagick=${tag_imagemagick}" >> "$GITHUB_OUTPUT"
          echo "tag_libvips=${tag_libvips}" >> "$GITHUB_OUTPUT"
          echo "tag_ffmpeg=${tag_ffmpeg}" >> "$GITHUB_OUTPUT"
          echo "purge_internal_tags_csv=${purge_internal_tags_csv}" >> "$GITHUB_OUTPUT"
          echo "purge_registry_tags_csv=${purge_registry_tags_csv}" >> "$GITHUB_OUTPUT"
          echo "tags_csv=${tags_csv}" >> "$GITHUB_OUTPUT"
          cat > ./run-boringcache-build.sh <<'SH'
          #!/usr/bin/env bash
          set -euo pipefail

          proxy_port=5000
          proxy_log="$(mktemp /tmp/boringcache-proxy.XXXXXX.log)"
          build_log="$(mktemp /tmp/boringcache-build.XXXXXX.log)"
          max_attempts=3
          cache_export_pattern='expected sha256:.*got sha256:e3b0|error writing layer blob|400 Bad Request|broken pipe'

          mode="${1:-full}"
          docker_registry_tag="${BORINGCACHE_TAG_DOCKER:-${BENCHMARK_ID}-docker-layers}"
          proxy_pid=""

          start_proxy() {
            : > "$proxy_log"
            RUST_LOG=info boringcache docker-registry "$BENCHMARK_WORKSPACE" "$docker_registry_tag" --host 0.0.0.0 --port "$proxy_port" --no-git --verbose > "$proxy_log" 2>&1 &
            proxy_pid=$!
          }

          stop_proxy() {
            if [[ -n "${proxy_pid:-}" ]] && kill -0 "$proxy_pid" >/dev/null 2>&1; then
              # SIGINT triggers graceful shutdown (flush pending uploads)
              kill -INT "$proxy_pid" >/dev/null 2>&1 || true
              # Wait up to 120s for proxy to flush cache data to backend
              local i=0
              while kill -0 "$proxy_pid" >/dev/null 2>&1 && [[ $i -lt 120 ]]; do
                sleep 1
                i=$((i + 1))
              done
              # Force kill if still running
              if kill -0 "$proxy_pid" >/dev/null 2>&1; then
                kill -9 "$proxy_pid" >/dev/null 2>&1 || true
              fi
              wait "$proxy_pid" 2>/dev/null || true
            fi
            proxy_pid=""
          }

          wait_proxy_ready() {
            local ready=0
            for _ in $(seq 1 60); do
              code="$(curl -s -o /dev/null -w '%{http_code}' "http://127.0.0.1:${proxy_port}/v2/" || true)"
              if [[ "$code" == "200" || "$code" == "401" ]]; then
                ready=1
                break
              fi
              sleep 1
            done
            [[ "$ready" -eq 1 ]]
          }

          cleanup() {
            stop_proxy
          }
          trap cleanup EXIT

          attempt=1
          while true; do
            cache_args=()
            internal_cache_enabled="1"
            if [[ "$mode" == "full" ]]; then
              cache_args=(
                --cache-from "type=registry,ref=127.0.0.1:${proxy_port}/${CACHE_SCOPE}:buildcache,registry.insecure=true"
                --cache-to "type=registry,ref=127.0.0.1:${proxy_port}/${CACHE_SCOPE}:buildcache,mode=max,registry.insecure=true"
              )
            elif [[ "$mode" == "seed-cache" ]]; then
              # --no-cache is required for type=registry export: without it, buildx
              # sees cached layers from the builder and skips pushing blobs to the
              # registry proxy, so the proxy never uploads to BC backend.
              cache_args=(
                --no-cache
                --cache-to "type=registry,ref=127.0.0.1:${proxy_port}/${CACHE_SCOPE}:buildcache,mode=max,registry.insecure=true"
              )
            elif [[ "$mode" == "partial-warm" ]]; then
              # Read-only: no --cache-to so stale timing measures rebuild + internal
              # BC cache only, without layer-export overhead (apples-to-apples with AC).
              cache_args=(
                --cache-from "type=registry,ref=127.0.0.1:${proxy_port}/${CACHE_SCOPE}:buildcache,registry.insecure=true"
              )
            elif [[ "$mode" == "internal-only" ]]; then
              cache_args=(--no-cache)
            elif [[ "$mode" == "internal-target" ]]; then
              cache_args=(--no-cache --target dev)
            else
              echo "Unknown build mode: $mode" >&2
              exit 1
            fi

            start_proxy
            if ! wait_proxy_ready; then
              echo "Registry proxy did not become ready (attempt ${attempt}/${max_attempts})" >&2
              tail -n 200 "$proxy_log" || true
              if [[ "$attempt" -ge "$max_attempts" ]]; then
                exit 1
              fi
              stop_proxy
              attempt=$((attempt + 1))
              sleep 3
              continue
            fi

            : > "$build_log"
            set +e
            DOCKER_BUILDKIT=1 docker buildx build \
              --builder "$BUILDER" \
              --file Dockerfile.boringcache \
              --tag "$IMAGE_TAG" \
              --progress=plain \
              "${cache_args[@]}" \
              --secret id=boringcache_token,env=BORINGCACHE_API_TOKEN \
              --build-arg BORINGCACHE_WORKSPACE="$BENCHMARK_WORKSPACE" \
              --build-arg BORINGCACHE_TAG_PNPM="$BORINGCACHE_TAG_PNPM" \
              --build-arg BORINGCACHE_TAG_MISE="$BORINGCACHE_TAG_MISE" \
              --build-arg BORINGCACHE_TAG_LIBHEIF="$BORINGCACHE_TAG_LIBHEIF" \
              --build-arg BORINGCACHE_TAG_LIBJXL="$BORINGCACHE_TAG_LIBJXL" \
              --build-arg BORINGCACHE_TAG_LIBRAW="$BORINGCACHE_TAG_LIBRAW" \
              --build-arg BORINGCACHE_TAG_IMAGEMAGICK="$BORINGCACHE_TAG_IMAGEMAGICK" \
              --build-arg BORINGCACHE_TAG_LIBVIPS="$BORINGCACHE_TAG_LIBVIPS" \
              --build-arg BORINGCACHE_TAG_FFMPEG="$BORINGCACHE_TAG_FFMPEG" \
              --build-arg BORINGCACHE_INTERNAL_CACHE_ENABLED="$internal_cache_enabled" \
              . 2>&1 | tee "$build_log"
            status=${PIPESTATUS[0]}
            set -e

            if [[ "$status" -eq 0 ]]; then
              if [[ "$mode" =~ ^(full|seed-cache)$ ]] && grep -Eq "$cache_export_pattern" "$build_log"; then
                stop_proxy
                if [[ "$attempt" -lt "$max_attempts" ]]; then
                  echo "Build succeeded but cache export was degraded (attempt ${attempt}/${max_attempts}); retrying to self-heal cache..." >&2
                  attempt=$((attempt + 1))
                  sleep 3
                  continue
                fi
                echo "Build succeeded but cache export still degraded after ${max_attempts} attempts; continuing run with best-effort cache state." >&2
              fi
              # Stop proxy gracefully so it can flush pending uploads
              if [[ "$mode" =~ ^(seed-cache|full)$ ]]; then
                echo "Flushing proxy cache to backend..."
                stop_proxy
              fi
              # Dump proxy log for diagnostics
              echo "=== Proxy log (${mode}, last 200 lines) ==="
              tail -n 200 "$proxy_log" 2>/dev/null || true
              echo "=== End proxy log ==="
              break
            fi

            stop_proxy

            if [[ "$attempt" -ge "$max_attempts" ]]; then
              echo "Build (${mode}) failed after ${max_attempts} attempts" >&2
              tail -n 200 "$build_log" || true
              tail -n 400 "$proxy_log" || true
              exit "$status"
            fi

            if grep -Eq "$cache_export_pattern" "$build_log"; then
              echo "Detected registry blob finalize mismatch (attempt ${attempt}/${max_attempts}); restarting proxy and retrying..." >&2
            else
              echo "Build (${mode}) failed (attempt ${attempt}/${max_attempts}); retrying..." >&2
            fi

            attempt=$((attempt + 1))
            sleep 3
          done
          SH
          chmod +x ./run-boringcache-build.sh

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            network=host

      - name: Purge BoringCache benchmark tags
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}
        run: |
          set -euo pipefail
          boringcache delete --no-git "$BENCHMARK_WORKSPACE" "${{ steps.scope.outputs.purge_registry_tags_csv }}" || true
          boringcache delete --no-platform --no-git "$BENCHMARK_WORKSPACE" "${{ steps.scope.outputs.purge_internal_tags_csv }}" || true

      - name: Run cold + seed cache build
        id: cold_baseline_build
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}
          BUILDER: ${{ steps.buildx.outputs.name }}
          CACHE_SCOPE: ${{ steps.scope.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM: ${{ steps.scope.outputs.pnpm_tag }}
          BORINGCACHE_TAG_MISE: ${{ steps.scope.outputs.mise_tag }}
          BORINGCACHE_TAG_LIBHEIF: ${{ steps.scope.outputs.tag_libheif }}
          BORINGCACHE_TAG_LIBJXL: ${{ steps.scope.outputs.tag_libjxl }}
          BORINGCACHE_TAG_LIBRAW: ${{ steps.scope.outputs.tag_libraw }}
          BORINGCACHE_TAG_IMAGEMAGICK: ${{ steps.scope.outputs.tag_imagemagick }}
          BORINGCACHE_TAG_LIBVIPS: ${{ steps.scope.outputs.tag_libvips }}
          BORINGCACHE_TAG_FFMPEG: ${{ steps.scope.outputs.tag_ffmpeg }}
          IMAGE_TAG: immich-server-benchmark:latest
        run: |
          set -euo pipefail
          start="$(date +%s)"
          ./run-boringcache-build.sh seed-cache
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Prune builder for re-seed
        run: docker buildx prune -f --builder "${{ steps.buildx.outputs.name }}"

      - name: Re-seed with populated BC restore layers
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}
          BUILDER: ${{ steps.buildx.outputs.name }}
          CACHE_SCOPE: ${{ steps.scope.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM: ${{ steps.scope.outputs.pnpm_tag }}
          BORINGCACHE_TAG_MISE: ${{ steps.scope.outputs.mise_tag }}
          BORINGCACHE_TAG_LIBHEIF: ${{ steps.scope.outputs.tag_libheif }}
          BORINGCACHE_TAG_LIBJXL: ${{ steps.scope.outputs.tag_libjxl }}
          BORINGCACHE_TAG_LIBRAW: ${{ steps.scope.outputs.tag_libraw }}
          BORINGCACHE_TAG_IMAGEMAGICK: ${{ steps.scope.outputs.tag_imagemagick }}
          BORINGCACHE_TAG_LIBVIPS: ${{ steps.scope.outputs.tag_libvips }}
          BORINGCACHE_TAG_FFMPEG: ${{ steps.scope.outputs.tag_ffmpeg }}
          IMAGE_TAG: immich-server-benchmark:latest
        run: |
          set -euo pipefail
          echo "Re-seeding: BC entries now exist from pass 1, so restore layers will have populated caches"
          ./run-boringcache-build.sh seed-cache


  scenario-builds:
    name: Scenario (${{ matrix.phase }}) (BoringCache)
    needs: seed-cache
    runs-on: ubuntu-latest
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        phase: [warm1, warm2, stale, layer_miss]
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          repository: ${{ env.PROJECT_REPO }}
          ref: ${{ env.PROJECT_REF }}

      - name: Checkout benchmark config
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.sha }}
          path: .benchmark-config

      - name: Setup BoringCache CLI
        uses: boringcache/setup-boringcache@v1
        with:
          token: ${{ secrets.BORINGCACHE_API_TOKEN }}
          platform: debian-bookworm-amd64
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}

      - name: Prepare benchmark build inputs
        id: scope
        run: |
          cp "$(which boringcache)" ./boringcache-bin
          echo '!boringcache-bin' >> .dockerignore
          echo '!libjxl-patches' >> .dockerignore
          cp .benchmark-config/dockerfiles/immich/Dockerfile.boringcache Dockerfile.boringcache
          cp -r .benchmark-config/dockerfiles/immich/libjxl-patches ./libjxl-patches

          cache_scope="${BENCHMARK_ID}-run-r${GITHUB_RUN_ID}-a${GITHUB_RUN_ATTEMPT}"
          docker_registry_tag="${BENCHMARK_ID}-docker-layers"
          pnpm_tag="immich-pnpm"
          mise_tag="immich-mise"
          tag_libheif="immich-libheif-1.20.2"
          tag_libjxl="immich-libjxl-0.11.1"
          tag_libraw="immich-libraw-0.22.0"
          tag_imagemagick="immich-imagemagick-7.1.2-2"
          tag_libvips="immich-libvips-8.17.3"
          tag_ffmpeg="immich-ffmpeg-7.1.3-1"
          internal_platform_suffix="debian-13-x86_64"
          purge_internal_tags_csv="${pnpm_tag}-${internal_platform_suffix},${mise_tag}-${internal_platform_suffix},${tag_libheif}-${internal_platform_suffix},${tag_libjxl}-${internal_platform_suffix},${tag_libraw}-${internal_platform_suffix},${tag_imagemagick}-${internal_platform_suffix},${tag_libvips}-${internal_platform_suffix},${tag_ffmpeg}-${internal_platform_suffix}"
          purge_registry_tags_csv="${docker_registry_tag}"
          tags_csv="${pnpm_tag},${mise_tag},${tag_libheif},${tag_libjxl},${tag_libraw},${tag_imagemagick},${tag_libvips},${tag_ffmpeg},${cache_scope},${docker_registry_tag}"
          echo "cache_scope=${cache_scope}" >> "$GITHUB_OUTPUT"
          echo "docker_registry_tag=${docker_registry_tag}" >> "$GITHUB_OUTPUT"
          echo "pnpm_tag=${pnpm_tag}" >> "$GITHUB_OUTPUT"
          echo "mise_tag=${mise_tag}" >> "$GITHUB_OUTPUT"
          echo "tag_libheif=${tag_libheif}" >> "$GITHUB_OUTPUT"
          echo "tag_libjxl=${tag_libjxl}" >> "$GITHUB_OUTPUT"
          echo "tag_libraw=${tag_libraw}" >> "$GITHUB_OUTPUT"
          echo "tag_imagemagick=${tag_imagemagick}" >> "$GITHUB_OUTPUT"
          echo "tag_libvips=${tag_libvips}" >> "$GITHUB_OUTPUT"
          echo "tag_ffmpeg=${tag_ffmpeg}" >> "$GITHUB_OUTPUT"
          echo "purge_internal_tags_csv=${purge_internal_tags_csv}" >> "$GITHUB_OUTPUT"
          echo "purge_registry_tags_csv=${purge_registry_tags_csv}" >> "$GITHUB_OUTPUT"
          echo "tags_csv=${tags_csv}" >> "$GITHUB_OUTPUT"
          cat > ./run-boringcache-build.sh <<'SH'
          #!/usr/bin/env bash
          set -euo pipefail

          proxy_port=5000
          proxy_log="$(mktemp /tmp/boringcache-proxy.XXXXXX.log)"
          build_log="$(mktemp /tmp/boringcache-build.XXXXXX.log)"
          max_attempts=3
          cache_export_pattern='expected sha256:.*got sha256:e3b0|error writing layer blob|400 Bad Request|broken pipe'

          mode="${1:-full}"
          docker_registry_tag="${BORINGCACHE_TAG_DOCKER:-${BENCHMARK_ID}-docker-layers}"
          proxy_pid=""

          start_proxy() {
            : > "$proxy_log"
            RUST_LOG=info boringcache docker-registry "$BENCHMARK_WORKSPACE" "$docker_registry_tag" --host 0.0.0.0 --port "$proxy_port" --no-git --verbose > "$proxy_log" 2>&1 &
            proxy_pid=$!
          }

          stop_proxy() {
            if [[ -n "${proxy_pid:-}" ]] && kill -0 "$proxy_pid" >/dev/null 2>&1; then
              # SIGINT triggers graceful shutdown (flush pending uploads)
              kill -INT "$proxy_pid" >/dev/null 2>&1 || true
              # Wait up to 120s for proxy to flush cache data to backend
              local i=0
              while kill -0 "$proxy_pid" >/dev/null 2>&1 && [[ $i -lt 120 ]]; do
                sleep 1
                i=$((i + 1))
              done
              # Force kill if still running
              if kill -0 "$proxy_pid" >/dev/null 2>&1; then
                kill -9 "$proxy_pid" >/dev/null 2>&1 || true
              fi
              wait "$proxy_pid" 2>/dev/null || true
            fi
            proxy_pid=""
          }

          wait_proxy_ready() {
            local ready=0
            for _ in $(seq 1 60); do
              code="$(curl -s -o /dev/null -w '%{http_code}' "http://127.0.0.1:${proxy_port}/v2/" || true)"
              if [[ "$code" == "200" || "$code" == "401" ]]; then
                ready=1
                break
              fi
              sleep 1
            done
            [[ "$ready" -eq 1 ]]
          }

          cleanup() {
            stop_proxy
          }
          trap cleanup EXIT

          attempt=1
          while true; do
            cache_args=()
            internal_cache_enabled="1"
            if [[ "$mode" == "full" ]]; then
              cache_args=(
                --cache-from "type=registry,ref=127.0.0.1:${proxy_port}/${CACHE_SCOPE}:buildcache,registry.insecure=true"
                --cache-to "type=registry,ref=127.0.0.1:${proxy_port}/${CACHE_SCOPE}:buildcache,mode=max,registry.insecure=true"
              )
            elif [[ "$mode" == "seed-cache" ]]; then
              # --no-cache is required for type=registry export: without it, buildx
              # sees cached layers from the builder and skips pushing blobs to the
              # registry proxy, so the proxy never uploads to BC backend.
              cache_args=(
                --no-cache
                --cache-to "type=registry,ref=127.0.0.1:${proxy_port}/${CACHE_SCOPE}:buildcache,mode=max,registry.insecure=true"
              )
            elif [[ "$mode" == "partial-warm" ]]; then
              # Read-only: no --cache-to so stale timing measures rebuild + internal
              # BC cache only, without layer-export overhead (apples-to-apples with AC).
              cache_args=(
                --cache-from "type=registry,ref=127.0.0.1:${proxy_port}/${CACHE_SCOPE}:buildcache,registry.insecure=true"
              )
            elif [[ "$mode" == "internal-only" ]]; then
              cache_args=(--no-cache)
            elif [[ "$mode" == "internal-target" ]]; then
              cache_args=(--no-cache --target dev)
            else
              echo "Unknown build mode: $mode" >&2
              exit 1
            fi

            start_proxy
            if ! wait_proxy_ready; then
              echo "Registry proxy did not become ready (attempt ${attempt}/${max_attempts})" >&2
              tail -n 200 "$proxy_log" || true
              if [[ "$attempt" -ge "$max_attempts" ]]; then
                exit 1
              fi
              stop_proxy
              attempt=$((attempt + 1))
              sleep 3
              continue
            fi

            : > "$build_log"
            set +e
            DOCKER_BUILDKIT=1 docker buildx build \
              --builder "$BUILDER" \
              --file Dockerfile.boringcache \
              --tag "$IMAGE_TAG" \
              --progress=plain \
              "${cache_args[@]}" \
              --secret id=boringcache_token,env=BORINGCACHE_API_TOKEN \
              --build-arg BORINGCACHE_WORKSPACE="$BENCHMARK_WORKSPACE" \
              --build-arg BORINGCACHE_TAG_PNPM="$BORINGCACHE_TAG_PNPM" \
              --build-arg BORINGCACHE_TAG_MISE="$BORINGCACHE_TAG_MISE" \
              --build-arg BORINGCACHE_TAG_LIBHEIF="$BORINGCACHE_TAG_LIBHEIF" \
              --build-arg BORINGCACHE_TAG_LIBJXL="$BORINGCACHE_TAG_LIBJXL" \
              --build-arg BORINGCACHE_TAG_LIBRAW="$BORINGCACHE_TAG_LIBRAW" \
              --build-arg BORINGCACHE_TAG_IMAGEMAGICK="$BORINGCACHE_TAG_IMAGEMAGICK" \
              --build-arg BORINGCACHE_TAG_LIBVIPS="$BORINGCACHE_TAG_LIBVIPS" \
              --build-arg BORINGCACHE_TAG_FFMPEG="$BORINGCACHE_TAG_FFMPEG" \
              --build-arg BORINGCACHE_INTERNAL_CACHE_ENABLED="$internal_cache_enabled" \
              . 2>&1 | tee "$build_log"
            status=${PIPESTATUS[0]}
            set -e

            if [[ "$status" -eq 0 ]]; then
              if [[ "$mode" =~ ^(full|seed-cache)$ ]] && grep -Eq "$cache_export_pattern" "$build_log"; then
                stop_proxy
                if [[ "$attempt" -lt "$max_attempts" ]]; then
                  echo "Build succeeded but cache export was degraded (attempt ${attempt}/${max_attempts}); retrying to self-heal cache..." >&2
                  attempt=$((attempt + 1))
                  sleep 3
                  continue
                fi
                echo "Build succeeded but cache export still degraded after ${max_attempts} attempts; continuing run with best-effort cache state." >&2
              fi
              # Stop proxy gracefully so it can flush pending uploads
              if [[ "$mode" =~ ^(seed-cache|full)$ ]]; then
                echo "Flushing proxy cache to backend..."
                stop_proxy
              fi
              # Dump proxy log for diagnostics
              echo "=== Proxy log (${mode}, last 200 lines) ==="
              tail -n 200 "$proxy_log" 2>/dev/null || true
              echo "=== End proxy log ==="
              break
            fi

            stop_proxy

            if [[ "$attempt" -ge "$max_attempts" ]]; then
              echo "Build (${mode}) failed after ${max_attempts} attempts" >&2
              tail -n 200 "$build_log" || true
              tail -n 400 "$proxy_log" || true
              exit "$status"
            fi

            if grep -Eq "$cache_export_pattern" "$build_log"; then
              echo "Detected registry blob finalize mismatch (attempt ${attempt}/${max_attempts}); restarting proxy and retrying..." >&2
            else
              echo "Build (${mode}) failed (attempt ${attempt}/${max_attempts}); retrying..." >&2
            fi

            attempt=$((attempt + 1))
            sleep 3
          done
          SH
          chmod +x ./run-boringcache-build.sh

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            network=host

      - name: Prune builder cache for isolated scenario
        run: docker buildx prune -f --builder "${{ steps.buildx.outputs.name }}"
      - name: Run warm build 1
        id: warm1_build
        if: matrix.phase == 'warm1'
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}
          BUILDER: ${{ steps.buildx.outputs.name }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM: ${{ steps.scope.outputs.pnpm_tag }}
          BORINGCACHE_TAG_MISE: ${{ steps.scope.outputs.mise_tag }}
          BORINGCACHE_TAG_LIBHEIF: ${{ steps.scope.outputs.tag_libheif }}
          BORINGCACHE_TAG_LIBJXL: ${{ steps.scope.outputs.tag_libjxl }}
          BORINGCACHE_TAG_LIBRAW: ${{ steps.scope.outputs.tag_libraw }}
          BORINGCACHE_TAG_IMAGEMAGICK: ${{ steps.scope.outputs.tag_imagemagick }}
          BORINGCACHE_TAG_LIBVIPS: ${{ steps.scope.outputs.tag_libvips }}
          BORINGCACHE_TAG_FFMPEG: ${{ steps.scope.outputs.tag_ffmpeg }}
          IMAGE_TAG: immich-server-benchmark:latest
        run: |
          set -euo pipefail
          start="$(date +%s)"
          ./run-boringcache-build.sh partial-warm
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Run warm build 2
        id: warm2_build
        if: matrix.phase == 'warm2'
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}
          BUILDER: ${{ steps.buildx.outputs.name }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM: ${{ steps.scope.outputs.pnpm_tag }}
          BORINGCACHE_TAG_MISE: ${{ steps.scope.outputs.mise_tag }}
          BORINGCACHE_TAG_LIBHEIF: ${{ steps.scope.outputs.tag_libheif }}
          BORINGCACHE_TAG_LIBJXL: ${{ steps.scope.outputs.tag_libjxl }}
          BORINGCACHE_TAG_LIBRAW: ${{ steps.scope.outputs.tag_libraw }}
          BORINGCACHE_TAG_IMAGEMAGICK: ${{ steps.scope.outputs.tag_imagemagick }}
          BORINGCACHE_TAG_LIBVIPS: ${{ steps.scope.outputs.tag_libvips }}
          BORINGCACHE_TAG_FFMPEG: ${{ steps.scope.outputs.tag_ffmpeg }}
          IMAGE_TAG: immich-server-benchmark:latest
        run: |
          set -euo pipefail
          start="$(date +%s)"
          ./run-boringcache-build.sh partial-warm
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Run stale Docker cache build (code change, partial warm)
        id: stale_docker_build
        if: matrix.phase == 'stale'
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}
          BUILDER: ${{ steps.buildx.outputs.name }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM: ${{ steps.scope.outputs.pnpm_tag }}
          BORINGCACHE_TAG_MISE: ${{ steps.scope.outputs.mise_tag }}
          BORINGCACHE_TAG_LIBHEIF: ${{ steps.scope.outputs.tag_libheif }}
          BORINGCACHE_TAG_LIBJXL: ${{ steps.scope.outputs.tag_libjxl }}
          BORINGCACHE_TAG_LIBRAW: ${{ steps.scope.outputs.tag_libraw }}
          BORINGCACHE_TAG_IMAGEMAGICK: ${{ steps.scope.outputs.tag_imagemagick }}
          BORINGCACHE_TAG_LIBVIPS: ${{ steps.scope.outputs.tag_libvips }}
          BORINGCACHE_TAG_FFMPEG: ${{ steps.scope.outputs.tag_ffmpeg }}
          IMAGE_TAG: immich-server-benchmark:latest
        run: |
          set -euo pipefail
          stale_file="$(find server web -type f \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' \) | head -n1 || true)"
          if [[ -z "${stale_file}" ]]; then
            stale_file="README.md"
          fi
          if [[ ! -f "${stale_file}" ]]; then
            echo "Could not find a file to mutate for stale build" >&2
            exit 1
          fi
          printf '\n// benchmark stale mutation %s-%s\n' "${GITHUB_RUN_ID:-local}" "$(date +%s)" >> "${stale_file}"
          echo "Applied stale mutation to ${stale_file}"

          start="$(date +%s)"
          ./run-boringcache-build.sh partial-warm
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Run layer-miss build (no Docker cache, full image)
        id: internal_only_build
        if: matrix.phase == 'layer_miss'
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}
          BUILDER: ${{ steps.buildx.outputs.name }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM: ${{ steps.scope.outputs.pnpm_tag }}
          BORINGCACHE_TAG_MISE: ${{ steps.scope.outputs.mise_tag }}
          BORINGCACHE_TAG_LIBHEIF: ${{ steps.scope.outputs.tag_libheif }}
          BORINGCACHE_TAG_LIBJXL: ${{ steps.scope.outputs.tag_libjxl }}
          BORINGCACHE_TAG_LIBRAW: ${{ steps.scope.outputs.tag_libraw }}
          BORINGCACHE_TAG_IMAGEMAGICK: ${{ steps.scope.outputs.tag_imagemagick }}
          BORINGCACHE_TAG_LIBVIPS: ${{ steps.scope.outputs.tag_libvips }}
          BORINGCACHE_TAG_FFMPEG: ${{ steps.scope.outputs.tag_ffmpeg }}
          IMAGE_TAG: immich-server-benchmark:latest
        run: |
          set -euo pipefail
          start="$(date +%s)"
          ./run-boringcache-build.sh internal-only
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Capture scenario timing
        id: phase_timing
        run: |
          set -euo pipefail
          seconds=""
          case "${{ matrix.phase }}" in
            warm1)
              seconds="${{ steps.warm1_build.outputs.seconds }}"
              ;;
            warm2)
              seconds="${{ steps.warm2_build.outputs.seconds }}"
              ;;
            stale)
              seconds="${{ steps.stale_docker_build.outputs.seconds }}"
              ;;
            layer_miss)
              seconds="${{ steps.internal_only_build.outputs.seconds }}"
              ;;
            *)
              echo "Unknown phase: ${{ matrix.phase }}" >&2
              exit 1
              ;;
          esac
          if [[ -z "${seconds}" ]]; then
            echo "No timing captured for phase ${{ matrix.phase }}" >&2
            exit 1
          fi
          echo "seconds=${seconds}" >> "$GITHUB_OUTPUT"
      - name: Persist scenario timing artifact
        run: |
          set -euo pipefail
          mkdir -p benchmark-phase
          cat > "benchmark-phase/${{ matrix.phase }}.env" <<EOF
          phase=${{ matrix.phase }}
          seconds=${{ steps.phase_timing.outputs.seconds }}
          EOF
      - name: Upload scenario timing artifact
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-phase-${{ env.BENCHMARK_ID }}-boringcache-${{ matrix.phase }}
          path: benchmark-phase/${{ matrix.phase }}.env
          if-no-files-found: error

  finalize-benchmark:
    name: Finalize benchmark artifacts
    needs: [seed-cache, scenario-builds]
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout benchmark config
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.sha }}
          path: .benchmark-config

      - name: Setup BoringCache CLI
        uses: boringcache/setup-boringcache@v1
        with:
          token: ${{ secrets.BORINGCACHE_API_TOKEN }}
          platform: debian-bookworm-amd64
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}

      - name: Download scenario timing artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: benchmark-phase-${{ env.BENCHMARK_ID }}-boringcache-*
          path: benchmark-phase
          merge-multiple: true
      - name: Collect scenario timings
        id: timings
        run: |
          set -euo pipefail
          parse_phase() {
            local phase="$1"
            local file="benchmark-phase/${phase}.env"
            if [[ ! -f "$file" ]]; then
              echo "Missing timing artifact for phase ${phase}" >&2
              ls -R benchmark-phase || true
              exit 1
            fi
            local seconds
            seconds="$(grep "^seconds=" "$file" | head -n1 | cut -d= -f2-)"
            if [[ -z "${seconds}" ]]; then
              echo "Timing artifact for phase ${phase} has empty seconds" >&2
              cat "$file" >&2
              exit 1
            fi
            echo "${phase}_seconds=${seconds}" >> "$GITHUB_OUTPUT"
          }
          parse_phase warm1
          parse_phase warm2
          parse_phase stale
          parse_phase layer_miss
      - name: Measure BoringCache storage usage
        id: cache_size
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_API_TOKEN }}
        run: |
          tags_csv="${{ needs.seed-cache.outputs.tags_csv }}"
          bytes="$(.benchmark-config/scripts/sum-boringcache-check-sizes.sh "$BENCHMARK_WORKSPACE" "$tags_csv")"
          echo "bytes=${bytes}" >> "$GITHUB_OUTPUT"

      - name: Write benchmark artifacts
        id: artifact_files
        run: |
          .benchmark-config/scripts/write-benchmark-artifacts.sh \
            --benchmark "$BENCHMARK_ID" \
            --strategy "boringcache" \
            --project-repo "$PROJECT_REPO" \
            --project-ref "$PROJECT_REF" \
            --cold-seconds "${{ needs.seed-cache.outputs.cold_seconds }}" \
            --warm1-seconds "${{ steps.timings.outputs.warm1_seconds }}" \
            --warm2-seconds "${{ steps.timings.outputs.warm2_seconds }}" \
            --stale-docker-seconds "${{ steps.timings.outputs.stale_seconds }}" \
            --internal-only-warm-seconds "${{ steps.timings.outputs.layer_miss_seconds }}" \
            --cache-storage-bytes "${{ steps.cache_size.outputs.bytes }}" \
            --cache-storage-source "boringcache-check" \
            --bytes-uploaded "${{ steps.cache_size.outputs.bytes }}" \
            --hit-behavior-note "Two-pass seed: pass 1 builds cold with --no-cache + cache-to via BC registry proxy (mode=max, BORINGCACHE_INTERNAL_CACHE_ENABLED=1) to create BC entries and record cold timing. Pass 2 prunes buildx and rebuilds with --no-cache + cache-to so BC restore layers find entries from pass 1 and exported layers have populated caches. Warm1/Warm2 use cache-from only (read-only, no layer export) with internal BC cache active. Stale Docker run mutates one TS/JS source file and rebuilds with cache-from only (read-only, no layer export) with internal BC cache enabled. Internal-only run uses --no-cache (full image, no Docker layers) with internal boringcache restore/save active. All scenario phases run as isolated matrix jobs on fresh runners with buildx pruned before building."

      - name: Upload benchmark artifacts
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-${{ env.BENCHMARK_ID }}-boringcache
          path: |
            ${{ steps.artifact_files.outputs.json_path }}
            ${{ steps.artifact_files.outputs.md_path }}
          if-no-files-found: error

      - name: Publish summary
        run: cat "${{ steps.artifact_files.outputs.md_path }}" >> "$GITHUB_STEP_SUMMARY"

      - name: Storage comparison with GHA cache
        if: always() && steps.cache_size.outputs.bytes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          bc_bytes="${{ steps.cache_size.outputs.bytes }}"

          # Query GHA cache usage for this repo
          gha_bytes="$(gh api "repos/${GITHUB_REPOSITORY}/actions/cache/usage" --jq '.active_caches_size_in_bytes' 2>/dev/null)" || {
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "_Storage comparison skipped (GHA API unreachable)_" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          }

          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "### Storage comparison" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Cache | Storage | Notes |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---|---|---|" >> "$GITHUB_STEP_SUMMARY"
          echo "| BoringCache | $(numfmt --to=iec-i --suffix=B "${bc_bytes}") | Immich benchmark tags (content-addressed, deduped across branches) |" >> "$GITHUB_STEP_SUMMARY"
          echo "| GitHub Actions | $(numfmt --to=iec-i --suffix=B "${gha_bytes}") | Repo total (buildkit blobs shared across projects, no cross-branch dedup) |" >> "$GITHUB_STEP_SUMMARY"
